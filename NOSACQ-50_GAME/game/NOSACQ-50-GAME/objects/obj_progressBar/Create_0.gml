// Configuración de la barra
bar_x = 20;
bar_y = 20;
bar_w = 300;
bar_h = 20;

// Colores
bar_bg_color = c_navy;
bar_fill_color = c_gray;
bar_border_color = c_white;

// Cantidad total de preguntas (usa la global)
total_questions = array_length(global.questions);

// En Create:
display_progress = 0;